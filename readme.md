### Hybrid Research Agent built using LangGraph

This system include two retrieval methods, `Semantic retrieval` and  `Internet retrieval using Tavily search`. Based on specific condition, either any one selected or both work parallel, to get more information.  

This project can run using streamlit frontend using fastapi backend and also using the langgraph studio which gives you a very beautiful interface to interact with the graph.


